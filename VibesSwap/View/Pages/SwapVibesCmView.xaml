<UserControl x:Class="VibesSwap.View.Pages.SwapVibesCmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:model="clr-namespace:VibesSwap.Model.Dimensional"
             mc:Ignorable="d" 
             d:DesignHeight="1500" 
             d:DesignWidth="1920">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Text="Swap VIBES CM's"
                   Style="{StaticResource PageHeader}"/>

        <Button Grid.Row="0"
                Grid.Column="1"
                Content="Refresh CM Data"
                Style="{StaticResource FormButton}"
                Command="{Binding RefreshCommand}"/>

        <!--#region Exec-->
        
        <Grid Grid.Row="1"
              Grid.Column="0"
              Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"
                                  MinWidth="200 "/>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="2*"
                                  MinWidth="400"/>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="3*"
                                  MinWidth="400"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Style="{StaticResource PageSubHeader}"
                       Text="EXEC" />

            <ListBox Grid.Row="1"
                     Grid.Column="0"
                     ItemsSource="{Binding HostsDisplayExec, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     SelectedItem="{Binding SelectedHostExec, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource FormListBox}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <GridSplitter Grid.Row="0"
                          Grid.Column="1"
                          Grid.RowSpan="2"
                          Width="3"
                          HorizontalAlignment="Center"
                          Background="LightGray" />

            <StackPanel Grid.Row="0"
                        Grid.Column="2"
                        Orientation="Horizontal">
                <Button Content="Poll All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PollAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.EXEC}"/>
                <Button Content="Start All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StartAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.EXEC}"/>
                <Button Content="Stop All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StopAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.EXEC}"/>
                <Button Content="Swap All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwapAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.EXEC}"/>
                <Button Content="Get All Props"
                        Style="{StaticResource FormButton}"
                        Command="{Binding GetAllPropsCommand}"
                        CommandParameter="{x:Static  model:HostTypes.EXEC}"/>
                <Button Content="Get Hosts"
                        Style="{StaticResource FormButton}"
                        Command="{Binding GetHostsCommand}"
                        CommandParameter="{x:Static model:HostTypes.EXEC}" />
                <Button Content="Set Prod Hosts"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SetProdHostsCommand}"
                        CommandParameter="{x:Static model:HostTypes.EXEC}" />
                <Button Content="Set Hlc Hosts"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SetHlcHostsCommand}"
                        CommandParameter="{x:Static model:HostTypes.EXEC}" />
            </StackPanel>
            
            <DataGrid Grid.Row="1"
                      Grid.Column="2"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      AutoGenerateColumns="False"
                      ItemsSource="{Binding CmsDisplayExec, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      SelectedItem="{Binding SelectedCmExec, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      Style="{StaticResource FormDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="CM Type"
                                        Width="Auto"
                                        IsReadOnly="True"
                                        Binding="{Binding CmType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="CM"
                                        Width="*"
                                        IsReadOnly="True"
                                        Binding="{Binding CmResourceName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="CM Status"
                                        Width="Auto"
                                        MinWidth="120"
                                        IsReadOnly="True"
                                        Binding="{Binding CmStatus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Row="0"
                          Grid.Column="3"
                          Grid.RowSpan="2"
                          Width="3"
                          HorizontalAlignment="Center"
                          Background="LightGray" />

            <StackPanel Grid.Row="0"
                        Grid.Column="4"
                        Orientation="Horizontal">
                <Button Content="Poll CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PollCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.EXEC}"/>
                <Button Content="Start CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StartCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.EXEC}"/>
                <Button Content="Stop CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StopCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.EXEC}"/>
                <Button Content="Swap CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwapCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.EXEC}"/>
                <Button Content="Get Properties"
                        Style="{StaticResource FormButton}"
                        Command="{Binding UpdatePropertiesCommand}"
                        CommandParameter="{x:Static model:HostTypes.EXEC}"/>
                <Separator Width="20" />
                <Button Content="Switch Terms"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwitchTermsCommand}"
                        CommandParameter="{x:Static model:HostTypes.EXEC}"/>
                <Button Content="Prepopulate Terms"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PrePopulateTermsCommand}"
                        CommandParameter="{x:Static model:HostTypes.EXEC}"/>
            </StackPanel>

            <DataGrid Grid.Row="1"
                      Grid.Column="4"
                      ItemsSource="{Binding SelectedCmExec.DeploymentProperties}"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      AutoGenerateColumns="False"
                      Style="{StaticResource FormDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Property"
                                        IsReadOnly="True"
                                        Width="Auto"
                                        Binding="{Binding PropertyKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Value"
                                        IsReadOnly="True"
                                        Width="500"
                                        Binding="{Binding PropertyValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Search Pattern"
                                        IsReadOnly="False"
                                        Width="*"
                                        MinWidth="200"
                                        Binding="{Binding SearchPattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Replace Pattern"
                                        IsReadOnly="False"
                                        Width="*"
                                        MinWidth="200"
                                        Binding="{Binding ReplacePattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!--#endregion-->
        
        <!--#region OperDb-->
        
        <Grid Grid.Row="2"
              Grid.Column="0"
              Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"
                                  MinWidth="200 "/>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="2*"
                                  MinWidth="400"/>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="3*"
                                  MinWidth="400"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Style="{StaticResource PageSubHeader}"
                       Text="OPERDB" />

            <ListBox Grid.Row="1"
                     Grid.Column="0"
                     ItemsSource="{Binding HostsDisplayOperDb, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     SelectedItem="{Binding SelectedHostOperDb, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource FormListBox}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <GridSplitter Grid.Row="0"
                          Grid.Column="1"
                          Grid.RowSpan="2"
                          Width="3"
                          HorizontalAlignment="Center"
                          Background="LightGray" />

            <StackPanel Grid.Row="0"
                        Grid.Column="2"
                        Orientation="Horizontal">
                <Button Content="Poll All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PollAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERDB}"/>
                <Button Content="Start All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StartAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERDB}"/>
                <Button Content="Stop All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StopAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERDB}"/>
                <Button Content="Swap All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwapAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERDB}"/>
                <Button Content="Get All Props"
                        Style="{StaticResource FormButton}"
                        Command="{Binding GetAllPropsCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERDB}"/>
            </StackPanel>

            <DataGrid Grid.Row="1"
                      Grid.Column="2"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      AutoGenerateColumns="False"
                      ItemsSource="{Binding CmsDisplayOperDb, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      SelectedItem="{Binding SelectedCmOperDb, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      Style="{StaticResource FormDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="CM Type"
                                        Width="Auto"
                                        IsReadOnly="True"
                                        Binding="{Binding CmType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="CM"
                                        Width="*"
                                        IsReadOnly="True"
                                        Binding="{Binding CmResourceName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="CM Status"
                                        Width="Auto"
                                        MinWidth="120"
                                        IsReadOnly="True"
                                        Binding="{Binding CmStatus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Row="0"
                          Grid.Column="3"
                          Grid.RowSpan="2"
                          Width="3"
                          HorizontalAlignment="Center"
                          Background="LightGray" />

            <StackPanel Grid.Row="0"
                        Grid.Column="4"
                        Orientation="Horizontal">
                <Button Content="Poll CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PollCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERDB}"/>
                <Button Content="Start CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StartCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERDB}"/>
                <Button Content="Stop CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StopCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERDB}"/>
                <Button Content="Swap CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwapCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERDB}"/>
                <Button Content="Get Properties"
                        Style="{StaticResource FormButton}"
                        Command="{Binding UpdatePropertiesCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERDB}"/>
                <Separator Width="20" />
                <Button Content="Switch Terms"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwitchTermsCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERDB}"/>
                <Button Content="Prepopulate Terms"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PrePopulateTermsCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERDB}"/>
            </StackPanel>

            <DataGrid Grid.Row="1"
                      Grid.Column="4"
                      ItemsSource="{Binding SelectedCmOperDb.DeploymentProperties}"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      AutoGenerateColumns="False"
                      Style="{StaticResource FormDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Property"
                                        IsReadOnly="True"
                                        Width="Auto"
                                        Binding="{Binding PropertyKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Value"
                                        IsReadOnly="True"
                                        Width="500"
                                        Binding="{Binding PropertyValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Search Pattern"
                                        IsReadOnly="False"
                                        Width="*"
                                        MinWidth="200"
                                        Binding="{Binding SearchPattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Replace Pattern"
                                        IsReadOnly="False"
                                        Width="*"
                                        MinWidth="200"
                                        Binding="{Binding ReplacePattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!--#endregion-->
        
        <!--#region OperAppOne-->
        
        <Grid Grid.Row="3"
              Grid.Column="0"
              Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"
                                  MinWidth="200 "/>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="2*"
                                  MinWidth="400"/>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="3*"
                                  MinWidth="400"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Style="{StaticResource PageSubHeader}"
                       Text="OPERAPP ONE" />

            <ListBox Grid.Row="1"
                     Grid.Column="0"
                     ItemsSource="{Binding HostsDisplayOperAppOne, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     SelectedItem="{Binding SelectedHostOperAppOne, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource FormListBox}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <GridSplitter Grid.Row="0"
                          Grid.Column="1"
                          Grid.RowSpan="2"
                          Width="3"
                          HorizontalAlignment="Center"
                          Background="LightGray" />

            <StackPanel Grid.Row="0"
                        Grid.Column="2"
                        Orientation="Horizontal">
                <Button Content="Poll All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PollAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP1}"/>
                <Button Content="Start All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StartAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP1}"/>
                <Button Content="Stop All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StopAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP1}"/>
                <Button Content="Swap All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwapAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP1}"/>
                <Button Content="Get All Props"
                        Style="{StaticResource FormButton}"
                        Command="{Binding GetAllPropsCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP1}"/>
                <Button Content="Get Hosts"
                        Style="{StaticResource FormButton}"
                        Command="{Binding GetHostsCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERAPP1}" />
                <Button Content="Set Prod Hosts"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SetProdHostsCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERAPP1}" />
                <Button Content="Set Hlc Hosts"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SetHlcHostsCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERAPP1}" />
            </StackPanel>
            
            <DataGrid Grid.Row="1"
                      Grid.Column="2"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      AutoGenerateColumns="False"
                      ItemsSource="{Binding CmsDisplayOperAppOne, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      SelectedItem="{Binding SelectedCmOperAppOne, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      Style="{StaticResource FormDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="CM Type"
                                        Width="Auto"
                                        IsReadOnly="True"
                                        Binding="{Binding CmType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="CM"
                                        Width="*"
                                        IsReadOnly="True"
                                        Binding="{Binding CmResourceName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="CM Status"
                                        Width="Auto"
                                        MinWidth="120"
                                        IsReadOnly="True"
                                        Binding="{Binding CmStatus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Row="0"
                          Grid.Column="3"
                          Grid.RowSpan="2"
                          Width="3"
                          HorizontalAlignment="Center"
                          Background="LightGray" />

            <StackPanel Grid.Row="0"
                        Grid.Column="4"
                        Orientation="Horizontal">
                <Button Content="Poll CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PollCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP1}"/>
                <Button Content="Start CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StartCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP1}"/>
                <Button Content="Stop CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StopCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP1}"/>
                <Button Content="Swap CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwapCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP1}"/>
                <Button Content="Get Properties"
                        Style="{StaticResource FormButton}"
                        Command="{Binding UpdatePropertiesCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERAPP1}"/>
                <Separator Width="20" />
                <Button Content="Switch Terms"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwitchTermsCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERAPP1}"/>
                <Button Content="Prepopulate Terms"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PrePopulateTermsCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERAPP1}"/>
            </StackPanel>

            <DataGrid Grid.Row="1"
                      Grid.Column="4"
                      ItemsSource="{Binding SelectedCmOperAppOne.DeploymentProperties}"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      AutoGenerateColumns="False"
                      Style="{StaticResource FormDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Property"
                                        IsReadOnly="True"
                                        Width="Auto"
                                        Binding="{Binding PropertyKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Value"
                                        IsReadOnly="True"
                                        Width="500"
                                        Binding="{Binding PropertyValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Search Pattern"
                                        IsReadOnly="False"
                                        Width="*"
                                        MinWidth="200"
                                        Binding="{Binding SearchPattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Replace Pattern"
                                        IsReadOnly="False"
                                        Width="*"
                                        MinWidth="200"
                                        Binding="{Binding ReplacePattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!--#endregion-->
        
        <!--#region OperAppTwo-->
        
        <Grid Grid.Row="4"
              Grid.Column="0"
              Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"
                                  MinWidth="200 "/>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="2*"
                                  MinWidth="400"/>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="3*"
                                  MinWidth="400"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Style="{StaticResource PageSubHeader}"
                       Text="OPERAPP TWO" />

            <ListBox Grid.Row="1"
                     Grid.Column="0"
                     ItemsSource="{Binding HostsDisplayOperAppTwo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     SelectedItem="{Binding SelectedHostOperAppTwo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource FormListBox}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <GridSplitter Grid.Row="0"
                          Grid.Column="1"
                          Grid.RowSpan="2"
                          Width="3"
                          HorizontalAlignment="Center"
                          Background="LightGray" />

            <StackPanel Grid.Row="0"
                        Grid.Column="2"
                        Orientation="Horizontal">
                <Button Content="Poll All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PollAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP2}"/>
                <Button Content="Start All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StartAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP2}"/>
                <Button Content="Stop All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StopAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP2}"/>
                <Button Content="Swap All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwapAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP2}"/>
                <Button Content="Get All Props"
                        Style="{StaticResource FormButton}"
                        Command="{Binding GetAllPropsCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP2}"/>
                <Button Content="Get Hosts"
                        Style="{StaticResource FormButton}"
                        Command="{Binding GetHostsCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERAPP2}" />
                <Button Content="Set Prod Hosts"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SetProdHostsCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERAPP2}" />
                <Button Content="Set Hlc Hosts"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SetHlcHostsCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERAPP2}" />
            </StackPanel>

            <DataGrid Grid.Row="1"
                      Grid.Column="2"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      AutoGenerateColumns="False"
                      ItemsSource="{Binding CmsDisplayOperAppTwo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      SelectedItem="{Binding SelectedCmOperAppTwo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      Style="{StaticResource FormDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="CM Type"
                                        Width="Auto"
                                        IsReadOnly="True"
                                        Binding="{Binding CmType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="CM"
                                        Width="*"
                                        IsReadOnly="True"
                                        Binding="{Binding CmResourceName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="CM Status"
                                        Width="Auto"
                                        MinWidth="120"
                                        IsReadOnly="True"
                                        Binding="{Binding CmStatus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Row="0"
                          Grid.Column="3"
                          Grid.RowSpan="2"
                          Width="3"
                          HorizontalAlignment="Center"
                          Background="LightGray" />

            <StackPanel Grid.Row="0"
                        Grid.Column="5"
                        Orientation="Horizontal">
                <Button Content="Poll CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PollCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP2}"/>
                <Button Content="Start CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StartCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP2}"/>
                <Button Content="Stop CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StopCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP2}"/>
                <Button Content="Swap CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwapCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.OPERAPP2}"/>
                <Button Content="Get Properties"
                        Style="{StaticResource FormButton}"
                        Command="{Binding UpdatePropertiesCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERAPP2}"/>
                <Separator Width="20" />
                <Button Content="Switch Terms"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwitchTermsCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERAPP2}"/>
                <Button Content="Prepopulate Terms"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PrePopulateTermsCommand}"
                        CommandParameter="{x:Static model:HostTypes.OPERAPP2}"/>
            </StackPanel>

            <DataGrid Grid.Row="1"
                      Grid.Column="5"
                      ItemsSource="{Binding SelectedCmOperAppTwo.DeploymentProperties}"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      AutoGenerateColumns="False"
                      Style="{StaticResource FormDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Property"
                                        IsReadOnly="True"
                                        Width="Auto"
                                        Binding="{Binding PropertyKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Value"
                                        IsReadOnly="True"
                                        Width="500"
                                        Binding="{Binding PropertyValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Search Pattern"
                                        IsReadOnly="False"
                                        Width="*"
                                        MinWidth="200"
                                        Binding="{Binding SearchPattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Replace Pattern"
                                        IsReadOnly="False"
                                        Width="*"
                                        MinWidth="200"
                                        Binding="{Binding ReplacePattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!--#endregion-->

        <!--#region MS-->

        <Grid Grid.Row="5"
              Grid.Column="0"
              Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"
                                  MinWidth="200 "/>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="2*"
                                  MinWidth="400"/>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="3*"
                                  MinWidth="400"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Style="{StaticResource PageSubHeader}"
                       Text="MS" />

            <ListBox Grid.Row="1"
                     Grid.Column="0"
                     ItemsSource="{Binding HostsDisplayMs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     SelectedItem="{Binding SelectedHostMs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource FormListBox}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <GridSplitter Grid.Row="0"
                          Grid.Column="1"
                          Grid.RowSpan="2"
                          Width="3"
                          HorizontalAlignment="Center"
                          Background="LightGray" />

            <StackPanel Grid.Row="0"
                        Grid.Column="2"
                        Orientation="Horizontal">
                <Button Content="Poll All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PollAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.MS}"/>
                <Button Content="Start All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StartAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.MS}"/>
                <Button Content="Stop All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StopAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.MS}"/>
                <Button Content="Swap All CM's"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwapAllCommand}"
                        CommandParameter="{x:Static  model:HostTypes.MS}"/>
                <Button Content="Get All Props"
                        Style="{StaticResource FormButton}"
                        Command="{Binding GetAllPropsCommand}"
                        CommandParameter="{x:Static  model:HostTypes.MS}"/>
            </StackPanel>

            <DataGrid Grid.Row="1"
                      Grid.Column="2"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      AutoGenerateColumns="False"
                      ItemsSource="{Binding CmsDisplayMs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      SelectedItem="{Binding SelectedCmMs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      Style="{StaticResource FormDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="CM Type"
                                        Width="Auto"
                                        IsReadOnly="True"
                                        Binding="{Binding CmType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="CM"
                                        Width="*"
                                        IsReadOnly="True"
                                        Binding="{Binding CmResourceName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="CM Status"
                                        Width="Auto"
                                        MinWidth="120"
                                        IsReadOnly="True"
                                        Binding="{Binding CmStatus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Row="0"
                          Grid.Column="3"
                          Grid.RowSpan="2"
                          Width="3"
                          HorizontalAlignment="Center"
                          Background="LightGray" />

            <StackPanel Grid.Row="0"
                        Grid.Column="4"
                        Orientation="Horizontal">
                <Button Content="Poll CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PollCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.MS}"/>
                <Button Content="Start CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StartCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.MS}"/>
                <Button Content="Stop CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding StopCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.MS}"/>
                <Button Content="Swap CM"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwapCmCommand}"
                        CommandParameter="{x:Static  model:HostTypes.MS}"/>
                <Button Content="Get Properties"
                        Style="{StaticResource FormButton}"
                        Command="{Binding UpdatePropertiesCommand}"
                        CommandParameter="{x:Static model:HostTypes.MS}"/>
                <Separator Width="20" />
                <Button Content="Switch Terms"
                        Style="{StaticResource FormButton}"
                        Command="{Binding SwitchTermsCommand}"
                        CommandParameter="{x:Static model:HostTypes.MS}"/>
                <Button Content="Prepopulate Terms"
                        Style="{StaticResource FormButton}"
                        Command="{Binding PrePopulateTermsCommand}"
                        CommandParameter="{x:Static model:HostTypes.MS}"/>
            </StackPanel>

            <DataGrid Grid.Row="1"
                      Grid.Column="4"
                      ItemsSource="{Binding SelectedCmMs.DeploymentProperties}"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      AutoGenerateColumns="False"
                      Style="{StaticResource FormDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Property"
                                        IsReadOnly="True"
                                        Width="Auto"
                                        Binding="{Binding PropertyKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Value"
                                        IsReadOnly="True"
                                        Width="500"
                                        Binding="{Binding PropertyValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Search Pattern"
                                        IsReadOnly="False"
                                        Width="*"
                                        MinWidth="200"
                                        Binding="{Binding SearchPattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <DataGridTextColumn Header="Replace Pattern"
                                        IsReadOnly="False"
                                        Width="*"
                                        MinWidth="200"
                                        Binding="{Binding ReplacePattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        
        <!--#endregion-->
    </Grid>
</UserControl>
